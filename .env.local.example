# Digital Ocean API token
DO_API_TOKEN=

# SSH key — pick ONE of these approaches:
# Option A: Path to your private key file
SSH_KEY_PATH=~/.ssh/id_rsa
# Option B: Raw private key contents (useful for CI/secrets managers)
# SSH_PRIVATE_KEY=

# --- OR use 1Password for everything ---
# OP_SERVICE_ACCOUNT_TOKEN=
# DO_API_TOKEN_OP_REF=op://AI-Agents/digital-ocean-api/credential
# (SSH keys resolved per-bot via op://AI-Agents/<bot> - SSH Private Key/private key)
